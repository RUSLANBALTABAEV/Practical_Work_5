#include <iostream>
#include <cmath>

using namespace std;

double S()
{
  double a, b, c, g, h, S1, S2, S3;
  S1 = 0.5 * c * c * sin(a) * cos(a);
  S2 = 0.5 * a * a * tan(b);
  S3 = 0.5 * h * h / (sin(g) * cos(g));
}

int main()
{
  setlocale(LC_ALL, "Russian");
  double a, b, c, g, h, S1, S2, S3;
  cout << ""; cin >> ;
  if () cout << "Площадь первого прямоугольного треугольника больше! ";
  if () cout << "Площадь второго прямоугольного треугольника больше! ";
  if () cout << "Площадь третьего прямоугольного треугольника больше! ";
  return 0;
}
